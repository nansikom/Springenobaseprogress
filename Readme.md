small side sprint to connect the frontend to the backend
npm install express
npm install cors 
Open index file
Run node server.js 
Make sure docker is started 
psql -h localhost -p 5434 -U postgres -d postgres 
input password 
make sure u have the functions in the plv8 for the server 
